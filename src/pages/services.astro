---
import BaseLayout from "../layouts/BaseLayout.astro";

const services = [
    {
        title: "Desarrollo Web",
        desc: "Sitios estáticos y dinámicos construidos para la era moderna.",
        icon: "🌐",
    },
    {
        title: "Optimización SEO",
        desc: "Aseguramos que tu contenido sea encontrado por quienes lo buscan.",
        icon: "🔍",
    },
    {
        title: "Consultoría IT",
        desc: "Asesoramiento técnico para escalar tus proyectos al siguiente nivel.",
        icon: "🚀",
    },
];
---

<BaseLayout
    title="Servicios"
    description="Explora lo que podemos hacer por ti."
>
    <section class="max-w-7xl mx-auto px-4 py-24 md:py-32 text-center">
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6">
            Nuestros Servicios
        </h1>
        <p class="text-xl text-gray-400 mb-16 max-w-2xl mx-auto">
            Soluciones integrales diseñadas para maximizar el potencial de tu
            presencia digital.
        </p>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {
                services.map((s) => (
                    <div class="group relative p-8 rounded-3xl bg-white/[0.03] border border-white/10 hover:bg-white/[0.05] transition-all overflow-hidden">
                        <div class="absolute -right-4 -top-4 text-8xl opacity-10 transition-transform group-hover:scale-110 group-hover:rotate-12 select-none">
                            {s.icon}
                        </div>
                        <div class="text-left relative z-10">
                            <div class="text-4xl mb-6">{s.icon}</div>
                            <h3 class="text-2xl font-bold text-white mb-4">
                                {s.title}
                            </h3>
                            <p class="text-gray-400 leading-relaxed">
                                {s.desc}
                            </p>
                            <button class="mt-8 text-blue-400 font-semibold flex items-center gap-2 group/btn hover:text-blue-300">
                                Saber más
                                <span class="transition-transform group-hover/btn:translate-x-1">
                                    →
                                </span>
                            </button>
                        </div>
                    </div>
                ))
            }
        </div>
    </section>
</BaseLayout>
